<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../../public/css/entreprise.css">

    <title>Document</title>
</head>

<body>


    <div class="container-fluid">
        <div class="row">

            <div class="col-md-2 p-0" id="sidebar-wrapper">
                {% include 'navbar.twig' %}
            </div>


            <div class="col-md-10 ml-auto" id="content">
                <div class="container-fluid">

                    <div class="form-container">
                        <form
                            action="{% if info.num_entreprise is not null %}../Controller/stagiaireEdit.php?id={{ info.num_entreprise }}{% else %}../Controller/stagiaireAjout.php{% endif %}"
                            method="post">
                            <div class="form-section">
                                <h3>Information</h3>

                                <div class="form-row">
                                    <label >Nom<span class="theStar">*</span> : </label>
                                    <input type="text" name="nom_resp" value="{{ info.nom_resp }}">
                                </div>
                                <div class="form-row">
                                    <label>Prenom<span class="theStar">*</span> : </label>
                                    <input type="text" name="nom_resp" value="{{ info.nom_resp }}">
                                </div>

                                <div class="form-row">
                                    <label>Nom d'utilisateur (8 caractères)<span class="theStar">*</span> : </label>
                                    <input type="text" name="nom_resp" value="{{ info.nom_resp }}">
                                </div>

                                <div class="form-row">
                                    <label >Mot de passe (8 caractères)<span class="theStar">*</span> : </label>
                                    <input type="text" name="nom_resp" value="{{ info.nom_resp }}">
                                </div>

                                <div class="form-row">
                                    <label >date<span class="theStar">*</span> : </label>
                                    <input type="date" name="nom_resp" value="{{ info.nom_resp }}">
                                </div>
                                
                            </div>
                        </form>


                        <div class="note">
                            Les champs comportant le symbole<span class="theStar">*</span> sont obligatoires
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>


</body>

</html>