<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../../public/css/entreprise.css">


    <title>Document</title>
</head>

<body>


    <div class="container-fluid">
        <div class="row">

            <div class="col-md-2 p-0" id="sidebar-wrapper">
                {% include 'navbar.twig' %}
            </div>


            <div class="col-md-10 ml-auto" id="content">
                <div class="container-fluid">


                    <h1 class="titre1"> {{ info.nom_etudiant }} {{ info.prenom_etudiant }} </h1>
                    <hr>

                    <h3>Information sur l'tudiant</h3>

                    <div class="container">
                        <div class="bubble">
                            <h4>Information générale: </h4>
                            <p><br>
                                Nom : {{ info.nom_etudiant }} <br>
                                Prenom : {{ info.prenom_etudiant }} <br>
                                classe: {{ info.nom_classe }} <br>
                                prof pricipal: {{ info.nom_professeur }} {{ info.prenom_professeur }} <br>
                            </p>
                        </div>
                        <div class="bubble">
                            <h4>Stages : </h4>
                            {% for stage in stages %}
                            <p>

                                {{ loop.index0+1 }}-Un stage dans l'entreprise
                                {{ stage.raison_sociale }}
                                de type {{ stage.type_stage }}
                                du {{ stage.debut_stage|slice(0, 9) }} au {{ stage.fin_stage|slice(0, 9) }} <br>
                                {% if stage.observation_stage is not empty %}
                                observation : {{ stage.observation_stage }} <br>
                                {% endif %}
                                {% if stage.desc_projet is not empty %}
                                description du projet réalisé : {{ stage.desc_projet|replace({"</br>": ""}) }}
                                {% endif %}
                             </p>   
                                {% endfor %}
                            
                        </div>

                    </div>

                    <br>
                    <hr>
                    <br>
                </div>
            </div>
        </div>
    </div>


</body>

</html>